import { Routes } from '@angular/router';
import { MainMenuPageComponent } from './pages/main-menu-page/main-menu-page.component';

export const routes: Routes = [{ component: MainMenuPageComponent, path: '' }];
